# 工作日志 - 持续面条化治理第三轮拆分frameview

## 上次工作结果
- 已完成 `app-handlers`、`timeline sortable`、`canvas`、`app.ts` 的第二轮拆分，lint 全部通过。

## 本次工作目标
- 继续收敛 UI 渲染层面条代码，把 `frameview` 中混合职责拆开，降低未来改布局/改交互的联动成本。

## 预期结果
- `frameview` 结构清晰，模块边界稳定，外部调用接口不变。
- `npm run lint` 继续全绿。

## 实际结果
- 已完成：
  - 新增 `src/modules/frameview/frame-panel-render.ts`（帧列表与动作列表渲染）。
  - 新增 `src/modules/frameview/export-preview-render.ts`（导出预览渲染）。
  - 新增 `src/modules/frameview/selected-info.ts`（选中信息文案构建）。
  - `src/modules/frameview/index.ts` 改为聚合导出，保留对外 API。
- 校验结果：
  - `npm run lint` 通过（TS/Rust/行数检查全部通过）。
