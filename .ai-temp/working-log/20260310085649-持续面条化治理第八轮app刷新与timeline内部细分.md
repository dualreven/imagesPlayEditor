# 工作日志 - 持续面条化治理第八轮app刷新与timeline内部细分

## 上次工作结果
- 已完成 frameview/dialogs/canvas 编辑控制与视口模块化拆分，lint 全绿。

## 本次工作目标
- 继续压入口与拖拽模块耦合：
  - 抽离 `app.ts` 的 refresh 流水。
  - 细分 timeline sortable 内部职责。
  - 继续拆分 layout 样式大文件。

## 预期结果
- `app.ts` 进一步靠近“组合根”。
- timeline 拖拽模块职责边界明确。
- 样式层级更细，降低联动风险。

## 实际结果
- 已完成：
  - 新增 `src/app-refresh-runtime/index.ts`，收敛主界面刷新编排；`app.ts` 改为装配主导。
  - timeline sortable 内部细分：
    - `src/modules/timeline/sortable-types.ts`
    - `src/modules/timeline/sortable-order.ts`
    - `src/modules/timeline/sortable-factories.ts`
    - `src/modules/timeline/sortable-system.ts` 改为组合层。
  - `layout.css` 改为聚合入口，新增：
    - `src/styles/layout/shell.css`
    - `src/styles/layout/headers.css`
    - `src/styles/layout/actions.css`
- 校验结果：
  - `npm run lint` 通过（TS/Rust/行数检查全部通过）。
