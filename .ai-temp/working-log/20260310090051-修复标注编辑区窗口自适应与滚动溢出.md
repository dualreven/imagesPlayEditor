# 工作日志 - 修复标注编辑区窗口自适应与滚动溢出

## 上次工作结果
- 已完成 app 刷新流水外提、timeline 拖拽内部细分、layout 样式分层，lint 全绿。

## 本次工作目标
- 修复标注编辑区尺寸不随窗口自适应，避免超出窗口与无意义滚动条。

## 预期结果
- 标注编辑区在有图/无图状态都能跟随窗口变化。
- 不再出现因容器盒模型导致的伪横向/纵向滚动条。

## 实际结果
- 已完成：
  - `src/styles/canvas.css`：
    - 修正 `stage-container` 盒模型为 `box-sizing: border-box`。
    - 去除 `min-width/min-height: 100%` 导致的 padding 溢出，改为 `min-width/min-height: 0` 与 `height: 100%`。
    - 增加 `overflow: hidden`，防止内部容器二次溢出。
  - `src/modules/canvas/stage-viewport.ts`：
    - 新增 `getViewportContentSize`、`applyViewportStageSize`。
  - `src/modules/canvas/index.ts`：
    - 初始化时即按视口设置 stage 尺寸。
    - `resize` 事件在无图状态下也会实时更新 stage 尺寸（不再固定 960x620）。
- 校验结果：
  - `npm run lint` 通过（TS/Rust/行数检查全部通过）。
