# 工作日志 - 切换标注工具自动取消帧选中

## 上次工作结果
- 已完成多轮面条化治理与 MSI 打包，当前 lint 全绿。

## 本次工作目标
- 调整交互默认行为：点击三种标注工具按钮时，自动取消当前帧/动作/标注选中，但用户仍可再次手动选中任意帧。

## 预期结果
- 点击 `框选/箭头/文字` 后不保留帧选中态。
- 不影响后续重新选帧与编辑流程。

## 实际结果
- 已完成：
  - 修改 `src/app-handlers/tool-style-handlers.ts`：
    - 在 `onToolSelect` 中，当工具为 `box/arrow/text` 时调用 `clearSelection(state)`。
    - `select` 工具不触发该自动清除逻辑。
- 校验结果：
  - `npm run lint` 通过（TS/Rust/行数检查全部通过）。
