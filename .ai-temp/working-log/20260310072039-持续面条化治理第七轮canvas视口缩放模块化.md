# 工作日志 - 持续面条化治理第七轮canvas视口缩放模块化

## 上次工作结果
- 已完成 frameview 内部拆分、dialogs 样式拆分、canvas 编辑控制分型拆分，lint 全绿。

## 本次工作目标
- 继续收敛 `canvas/index.ts` 的非核心职责，把视口与缩放计算逻辑抽离，减少控制器内部混杂。

## 预期结果
- `CanvasController` 只保留状态编排与调用，不承载大量视口工具逻辑。
- `npm run lint` 持续通过。

## 实际结果
- 已完成：
  - 新增 `src/modules/canvas/stage-viewport.ts`，收敛：
    - 图片尺寸读取
    - 样式参考尺寸计算
    - 场景坐标换算
    - 显示缩放应用
    - 自适应缩放计算
    - 原始比例导出
  - `src/modules/canvas/index.ts` 接入该模块，文件降至约 242 行。
- 校验结果：
  - `npm run lint` 通过（TS/Rust/行数检查全部通过）。
