# 工作日志

- 上次工作结果: 已完成 toolbar 与侧栏视觉整治，导出面板仍固定在主界面右侧。
- 本次工作目标: 去掉主界面 `export-panel`，在 toolbar 增加“导出”按钮，点击后弹出导出信息面板。
- 预期结果: 导出信息与导出按钮迁移到弹窗，现有导出逻辑不变，lint 通过。
- 实际结果: 已将主界面右侧 `export-panel` 移除，在 toolbar 新增 `#open-export-panel-btn`；新增 `#export-dialog` 承载原导出信息（`#selected-info` / `#export-feedback` / `#export-frame-list`）与导出动作按钮 `#export-btn`。`app-shell` 与 `app.ts` 已接入弹窗开关事件，`npm run lint` 全量通过（TS、Rust、行数检查）。
