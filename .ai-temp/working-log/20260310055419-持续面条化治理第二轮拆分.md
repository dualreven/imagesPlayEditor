# 工作日志 - 持续面条化治理第二轮拆分

## 上次工作结果
- 已完成样式分层、导出弹窗迁移、选中拖拽修复，并拆分 `app-events`、`app-handlers` 初版，`lint` 全绿。

## 本次工作目标
- 继续降低入口耦合与拖拽重建副作用，推进面条化治理到下一层：
  - 细分 `app-handlers` 为分域处理器。
  - 优化 timeline sortable 生命周期。
  - 继续拆分 `canvas/index.ts` 与 `app.ts` 的混合职责。

## 预期结果
- 行为不回归前提下，入口编排更清晰、模块职责更单一。
- `npm run lint` 持续通过。

## 实际结果
- 已完成：
  - `src/app-handlers` 拆分为 `dialog-handlers.ts`、`tool-style-handlers.ts`、`timeline-handlers.ts`，公共类型收敛到 `types.ts`。
  - `src/modules/timeline/sortable-system.ts` 改为“时间线实例单次创建 + action 列表按 frameId 增量同步”。
  - `src/modules/canvas/index.ts` 进一步拆分：
    - 新增 `drawing-flow.ts`（预览与提交）。
    - 新增 `text-input-overlay.ts`（文本输入覆盖层生命周期）。
  - `src/app.ts` 继续瘦身：
    - 新增 `src/app-ui-controls/index.ts` 收敛按钮/控件状态更新。
    - 新增 `src/app-state-ops/index.ts` 收敛状态变更操作（删帧/删动作/切换清空/切换独占）。
- 校验结果：
  - `npm run lint` 通过（TS/Rust/行数检查全部通过）。
