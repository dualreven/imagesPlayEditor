# 工作日志 - build版本号与git版本命名落地

## 上次工作结果
- 已完成“拖拽到新建帧落点自动建帧”与“帧高度紧凑化”，并通过 lint/build。

## 本次工作目标
- 将构建版本落到 `tauri:build`：支持可选版本参数，默认使用 `YY.MM.NN` 自动续号。
- 让 MSI 文件名强制带上 git 版本号（short hash）。

## 预期结果
- `npm run tauri:build -- --version 26.03.08` 可使用指定版本。
- `npm run tauri:build` 无参数时自动续号（同月 +1，不同月重置为 01）。
- 构建产物命名包含 git short hash。

## 实际结果
- 已完成：
  - 新增 `scripts/tauri-build.mjs`：
    - 参数解析：`--version/-v`（格式 `YY.MM.NN`）。
    - 自动续号：读取 `.ai-temp/memory-bank/build-version-state.json`。
    - 动态写入 `src-tauri/tauri.auto-build.conf.json` 的 `version`（semver）。
    - 调用原始 `tauri build` 后重命名 MSI，强制加入 `git short hash`。
  - 更新 `package.json`：
    - 新增 `_tauri:build:raw`。
    - `tauri:build` 改为执行 `node scripts/tauri-build.mjs`。
  - 更新 `README.md`：
    - 增加构建版本策略与命令示例。
    - 增加无 HEAD commit 时 failed-fast 说明。
- 校验结果：
  - `npm run lint` 通过。
  - `npm run build` 通过。
  - `npm run tauri:build` 当前仓库无 HEAD commit，按预期 failed-fast：
    - `Git HEAD not found. Please create at least one commit before build.`
