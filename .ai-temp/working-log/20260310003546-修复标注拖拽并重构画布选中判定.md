# 工作日志

- 上次工作结果: 样式层完成分层重构，但用户反馈“标注选中后无法拖拽”。
- 本次工作目标: 先修复拖拽失效，再做相关结构重构，降低画布交互回归风险。
- 预期结果: 选中标注在选择工具下可稳定拖拽；画布选中可编辑判定逻辑收敛为统一函数。
- 实际结果: 已修复拖拽失效并立即开始重构：
- 修复: `CanvasController` 的可编辑判定从“仅无可见过滤可编辑”调整为“选择工具下，若有可见过滤则允许编辑当前选中且可见的标注”，恢复选中拖拽能力。
- 重构: 新增 `src/modules/canvas/interaction-policy.ts`，将交互判定抽为纯函数；`canvas/index.ts` 改为调用策略函数，避免条件散落。
- 质量: `npm run lint` 全量通过（TS、Rust、行数检查）。
