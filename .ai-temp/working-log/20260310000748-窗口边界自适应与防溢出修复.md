# 工作日志

- 上次工作结果: 导出弹窗已重构，但用户反馈主界面仍会超出窗口底部。
- 本次工作目标: 让整体布局严格受窗口高度约束，禁止页面级溢出，仅允许面板内部滚动。
- 预期结果: 任意窗口尺寸下不出现超出窗口边界，时间线与画布区域自动收缩并内部滚动。
- 实际结果: 已并入后续样式分层重构实现：在 `layout.css`/`canvas.css` 中落地 `100dvh` 固定视口、页面级 `overflow: hidden`、面板内滚动策略；右侧时间线与画布区域均改为内部滚动，不再依赖页面滚动。相关改动最终在 `src/styles/` 分层体系中生效。
