# 工作日志 - 拖拽越界新建帧与帧高度紧凑化

## 上次工作结果
- 已完成工具切换时清空帧选中修复，并通过 `npm run lint`。

## 本次工作目标
- 新增行为：动作拖拽到帧区域外的新建落点时，释放后自动创建新帧并放入该动作。
- 调整帧高度策略：帧列表中的帧卡片应尽量紧凑，不拉伸填满剩余空间。

## 预期结果
- 拖拽动作时出现虚线“新建帧”落点，放下后自动建帧并选中该动作。
- 帧列表容器仍可滚动，但帧卡片保持内容高度，不被强制拉高。
- lint 与构建通过。

## 实际结果
- 已完成：
  - 新增 `src/modules/timeline/new-frame-dropzone.ts`，定义 `NEW_FRAME_DROPZONE_ID` 与识别函数。
  - 新增 `moveActionToNewFrame`（`src/modules/timeline/index.ts`），实现移除旧帧后追加新帧。
  - `src/app-timeline-runtime/index.ts` 增加新建帧落点分支：落下后自动建帧、选中动作并提示状态。
  - `src/modules/frameview/frame-panel-render.ts` 追加虚线新建帧占位卡。
  - 新增 `src/modules/frameview/frame-panel-new-frame-dropzone.ts` 封装占位卡 DOM。
  - `src/styles/timeline/actions.css` 增加新建帧虚线占位样式，仅在拖拽中显示。
  - `src/styles/timeline/panel.css` 增加 `align-content: start` + `grid-auto-rows: max-content`，帧高度改为紧凑策略。
  - `src/app-handlers/timeline-handlers.ts` 增加 `.frame-new-dropzone` 点击忽略，避免误触发背景取消选中。
- 校验结果：
  - `npm run lint` 通过。
  - `npm run build` 通过。
