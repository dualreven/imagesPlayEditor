# 工作日志

- 上次工作结果: 完成面条分析报告，确认最高风险在 `app.ts` 事件绑定与刷新耦合。
- 本次工作目标: 立即开始治理，先拆分 `app.ts` 事件绑定到独立模块，降低入口复杂度。
- 预期结果: `app.ts` 不再直接堆叠大量 `addEventListener`，改为单点绑定函数。
- 实际结果: 已完成第一阶段治理落地：
- 新增 `src/app-events/index.ts`，统一注册/解绑工具栏、设置、时间线、导出等 UI 事件。
- `src/app.ts` 移除大段 `addEventListener` 直接绑定，改为业务回调传入 `bindAppEvents`，入口耦合降低。
- 进一步将导出按钮 UI 流程抽到 `src/modules/export/index.ts` 的 `runExportWithUi`，减少入口导出流程样板代码。
- `src/app.ts` 行数控制在 487（<500），`npm run lint` 全量通过（TS、Rust、行数检查）。
