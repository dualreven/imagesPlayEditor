# 工作日志 - 整理CSS去重按钮状态选择器

## 上次工作结果
- 已完成标注编辑区自适应修复与持续面条化拆分，lint 全绿。

## 本次工作目标
- 对现有 CSS 做一次无视觉变更的整理，重点减少重复选择器与重复状态规则，提高可维护性。

## 预期结果
- CSS 规则重复度下降，结构更清晰，后续改动成本更低。
- `npm run lint` 持续通过。

## 实际结果
- 已完成：
  - `src/styles/toolbar/buttons.css`：
    - 使用 `:is(...)` 合并多个按钮集合选择器。
    - 统一 hover/active 规则声明，消除重复列表。
  - `src/styles/timeline/frame-card.css`：
    - 合并 `frame-exclusive/edit/delete` 按钮公共样式。
    - 保留独占按钮与编辑/删除按钮的差异化覆盖规则。
- 校验结果：
  - `npm run lint` 通过（TS/Rust/行数检查全部通过）。
