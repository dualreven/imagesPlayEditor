# 工作日志

- 上次工作结果: 导出面板已改为弹窗，用户反馈时间线帧列表未撑满到底部。
- 本次工作目标: 让帧列表区域在右侧卡片内自适应拉满，并保持内部滚动。
- 预期结果: 时间线卡片撑满侧栏高度，帧列表占据剩余空间并可滚动。
- 实际结果: 已在 `styles-ui-refresh.css` 增加侧栏与时间线卡片撑满规则：`.timeline-panel.side-card { flex: 1; min-height: 0; }`，并将 `.frame-list-panel` 改为占满剩余空间（`flex: 1; max-height: none;`）。`npm run lint` 全量通过（TS、Rust、行数检查）。
