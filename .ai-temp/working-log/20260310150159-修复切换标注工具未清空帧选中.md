# 工作日志 - 修复切换标注工具未清空帧选中

## 上次工作结果
- 已实现切换到标注工具时清理选中态，但用户反馈帧选中未被正确清空。

## 本次工作目标
- 修复行为偏差：点击 `box/arrow/text` 时必须明确清空帧选中，不只清空动作选中。

## 预期结果
- 三种标注工具按钮点击后，`selectedFrameId` 必为 `null`。
- 用户仍可后续手动重新选中任意帧。

## 实际结果
- 已完成：
  - 修改 `src/app-handlers/tool-style-handlers.ts`：
    - 在 `onToolSelect`（`box/arrow/text`）分支中显式执行：
      - `state.selectedFrameId = null`
      - `clearActionSelection(state)`
      - `state.showCurrentFrameOnly = false`
  - 避免仅依赖通用清理函数导致的行为歧义。
- 校验结果：
  - `npm run lint` 通过（TS/Rust/行数检查全部通过）。
